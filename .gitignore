# ACE-managed — do not edit this block.
# Managed by ACE. See https://github.com/ace-rs/ace
.agents/agents
.agents/commands
.agents/rules
.agents/skills
.claude/agents
.claude/commands
.claude/rules
.claude/skills
.opencode/agents
.opencode/commands
.opencode/rules
.opencode/skills
ace.local.toml
# end ACE

# ACE session scratch — trail, inbox, handoff log (runtime artifacts, never committed)
/.ace/

# Durable design record — kept local. This repo is public and the record carries
# maintainer-internal detail (personal school, tracker, machine paths). Agents still
# read and write it; it just never ships. See CLAUDE.md.
/.docs/
